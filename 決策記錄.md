# 決策記錄

滾動記錄已拍板的事，避免重複討論。未決事項列在最後。

> **去識別化版本。** 院區名稱一律以 A／B／C… 代號表示（A 院區＝試辦院區，
> 絕大多數實測數字都來自它）。內部資料庫表名、院區代號對照表、電話號碼、
> 設備清單檔名已移除或遮蔽。技術推理與實測數字完整保留。

---

## 已決定

### D-001　逐字稿：只留底，初期不開放查詢
**日期**：2026-09-01
**決定**：逐字稿全部保存，但**不提供任何查詢介面**（含網頁、全文檢索）。
**理由**：辨識品質尚待修正，開放查詢會讓半成品被當成事實引用。
**後續方向**：未來考慮開放給查詢者協助修正，累積人工校正語料後做模型回訓微調。
**對設計的影響**：
- 資料庫規劃裡的 `turns` 表照建、照存，但不建 FTS5 全文索引、不開查詢 API
- 校正語料必須以 **(音檔片段, 正確文字)** 成對保存才有微調價值 —— 現有的
  `04_transcript/*.json` 已含 `offsets`（毫秒起訖）與可回溯的 `rel_path`，格式已符合
- 一旦開放校正，校正者會看到病患個資 → 存取控制問題會提前發生，需先處理

### D-002　待辦事項：不設負責人，報表僅供抽查參考
**日期**：2026-09-01
**決定**：AI 產出的報表**定位為抽查參考**，不指派負責人、不做待辦追蹤。
**對設計的影響**：
- `followups` **不建 `status` / `closed_at` / `closed_by` 欄位** ——
  永遠是 open 的狀態欄會製造「有人在追蹤」的假象
- 不做「未完成待辦」告警
- 報表措辭改為描述性（「本月出現 N 次未完成事項」），不用「待處理」這種
  暗示有人該行動的字眼
- 履行率改用**資料自證**取代人工閉環（見 D-003）

### D-003　閉環方式：用通話紀錄自證，不靠人工標記
**日期**：2026-09-01
**決定**：承諾回電等事項的履行狀況，由「後續是否有撥給同一號碼」自動推算。
**依據**：2025-09 A 院區 2,734 通實測基準線 —— 來電後 1h 內回撥 4.6%、
24h 內 8.0%、72h 內 9.9%；該號碼整月曾被撥出 26.0%。
**已知限制**：客服可能改用手機或通訊軟體聯繫、號碼顯示可能不同 →
結果是**下限估計**，適合看趨勢，不適合追個案。

### D-004　號碼正規化規則
**日期**：2026-09-01
**發現**：撥出號碼帶一碼外線存取碼 `0`（例：`0212345678` ＝ 外線 0 + 市話 212345678），
來電號碼則是原始號碼。不正規化的話跨通話關聯分析會 **100% 比對失敗**（實測 0%）。
**規則**：撥出號碼若以 `0` 開頭且長度 ≥ 9，去掉第一個 `0` 再比對。
**適用**：任何跨通話的號碼關聯（回撥比對、重複來電、同一客戶軌跡）。

### D-006　`followups` 準確率驗證通過，維持現狀
**日期**：2026-09-02
**背景**：任務卡列為待驗證項——`followups` 從頭到尾只看過「清單看起來合理」，
沒有量化驗證過；且與 D-005 失敗的 `intents` 用了同一種措辭（「任何『我再回電』都要列入」），
擔心會有同一種「鼓勵多列」的病。
**做法**：A 院區 2025-09 全量 1,134 筆 `followups` 中隨機抽 20 筆，逐筆對照原始逐字稿判斷真陽性。
**結果**：19/20（95%）在逐字稿中找得到清楚對應語句，責任方分配大致合理，高於 7 成門檻。
唯一的誤判是把同一通電話裡兩件不同的事混成一件描述，屬於「歸類到錯的細節」，
不是憑空捏造，且是單一案例。
**結論**：`followups` 不比照 D-005 處理，維持現有 schema 與 prompt 用詞。
**副作用發現**：抽樣中有 1 通（662 秒）完整內容是訂民宿房型的對話，與客服業務完全無關，
判斷是資料源頭問題（線路被挪作他用或轉接誤入），非模型或 pipeline 缺陷，先記錄不處理。

### D-007　`num_ctx` 提高到 32768、加 `num_predict` 上限
**日期**：2026-09-02
**起因**：A 院區 9 通永久失敗，症狀都是 JSON 解不開（`Unterminated string` 等），
看起來像模型輸出品質差。
**量測**：用 3,925 通的 `_stats` 真實 token 數擬合出
`總token ≈ 3182 + 4.249 × 逐字稿字元數`；成功通話的 token 總量最大值 **16380**，
而當時 `num_ctx` 設 16384 —— 只差 4 個 token，證明有通話頂到天花板被截斷。
模型硬上限是 262144（Qwen3.5 27.3B／Q4_K_M），16384 是專案自己設的軟上限。
**決定**：`num_ctx` 16384 → 32768（實測 VRAM 21.8 GB／32.6 GB，仍有 10.7 GB 餘裕）。
**結果**：9 通救回 6 通，A 院區從 3,925 → 3,931 通（99.92%）。

**推翻的假設**：原本以為失敗是「逐字稿太長」。實際重跑後發現**救回的都是最長的**
（3,232～4,775 字元），**仍失敗的反而是最短的三通**（627／1,092／1,488 字元）。
其中 627 字元那通吐了 4 萬多字元 JSON 才被切斷 —— 是 grammar-constrained 輸出時的
**暴走生成**，與逐字稿長短無關。也驗證過與 whisper 重複無關：重複段佔比 72% 的那通
反而成功，失敗的三通重複佔比只有 0～8%。
**因此加上** `num_predict = 12000`（合法通話實測最多用到 7,871，給 50% 餘裕）。
不是為了省資源，是止血：暴走一次原本燒滿 num_ctx 再乘以 3 次重試約 17 分鐘，
加上限後 3 通 9 次嘗試合計只要 11.4 分鐘。
**並把錯誤訊息拆成兩種**：撞 `num_predict` 報「疑似暴走生成」、撞 `num_ctx` 報
「逐字稿過長」，避免下次再誤診成模型品質問題。
**殘留**：3 通（0.08%）仍因暴走失敗，接受。

### D-008　Ollama 升級 0.33.2，實測不影響資料可比性
**日期**：2026-09-03
**背景**：Ollama 自動更新程式每小時重試安裝 v0.33.2 但一直失敗（檔案被佔用，
會觸發 RestartManager 跳出「其他程式正在使用中」）。風險是它哪次在批次跑到一半時
成功，會直接砍掉 Ollama、中斷 7–8 小時的分析。因此挑無排程時段主動安裝。
**升級**：0.32.13 → 0.33.2（安裝檔經 Authenticode 驗簽）。
8 個模型全部保留，`num_ctx=32768` 仍生效。
**疑慮**：A、B 兩院區都在 0.32.13 分析，之後院區會在 0.33.2，若兩版輸出有系統性
差異，跨院區比較（如台語濃度對照）會被混淆。單通對照確實看到差異
（turns 11 vs 30、call_category 不同），但 temperature 0.3 下單一樣本無法歸因。
**驗證**：挑 10 通長且標籤多的通話（平均 617 秒／161 段 turns），在 0.33.2 跑兩次，
與既有 0.32.13 結果三方對照 ——

| | 同版兩次（純雜訊）| 舊 vs 新（版本＋雜訊）|
|---|---|---|
| 列舉欄位一致率 | 96% | 96% |
| 布林欄位一致率 | 98% | 98% |
| turns 平均差異 | 18.6 | 17.1 |

**結論**：跨版本差異完全落在同版本雜訊內，`call_category` 9 通三次執行全部一致。
**版本升級不造成系統性偏移，既有資料不需重跑，跨院區比較有效。**
先前那個單通差異是抽樣雜訊，不是版本效應。
**附帶觀察**：同一通在兩次執行中一次失敗一次成功，佐證「暴走生成」失敗是隨機的，
不是特定通話的固有缺陷（見 D-007）。

---

### D-009　PoC 不接內部資料庫；歸屬比對留給正式實作
**日期**：2026-09-15
**範圍界定**：PoC 階段**刻意不牽涉內部資料庫**，檔案歸屬（哪一通屬於哪個院區、
哪支分機）全部從**檔名**推導。正式實作時改由資料庫建檔比對，確認資料正確性。

**未來的取檔規劃**：由城市節點定期下載錄音檔案，落地後查表找到對應檔案與其分機資訊。
分機歸屬與放置位置可由內部 CTI 資料庫的三張表查詢（表名不列出）。

**這會補掉目前最大的一個缺口**：從檔名解出的版型是
`<線路別><3 碼院區代號><2 碼話機編號>`，但院區代號只推得出約一半，
另一半用約束推導釘不死 —— 工具索引因此寫著「只能拿內部院區代號表對，別再推」。

**★ 接表時第一件事：先拿已確定的那一半對照去驗，不要假設欄位對得上。**

理由是這個坑已經踩過一次：某份內部設備清單 xlsx 裡的話機 ID 欄位
**不等於**檔名裡的院區代號（同一個院區，清單 ID 是個位數、檔名代號卻是三位數，
而且兩者沒有換算關係）。當時假設兩者相同，從某個檔名推出的院區看起來完全合理，
實際上對應到的是完全不同的院區。內部資料庫中對應的那張表若就是那份 xlsx 的
來源，它的主鍵很可能同樣不是檔名那組碼。

**半吻合的規則比沒有規則更危險。** 沒有規則時你會去查；半吻合時你會直接相信。

**對現有程式的影響**（正式實作時要處理，PoC 階段不動）：
目前所有工具都假設來源結構是 `CTI/<院區>/<YYYYMMDD>/`
（含 `run_month.py` 的 `walk()`／`mirror_month()`）。改成城市分層後這些都要改。
FTP 下載規劃書 v0.1 的 `00_raw\<年>\<月>\<日>\` 也一併過時。

---

## 記著，之後再決定

### P-001　保留與銷毀排程
逐字稿與分析結果要不要跟原始錄音同一個銷毀排程？
`pii_types` 已逐通標記，技術上可做差異化保留（例如含個資者較短、純內部行政較長）。
需法遵核定，**目前不實作，先記著**。

### P-002　全量處理的 LLM 部署方式
全年約 4,452 小時音訊。地端 Qwen 需約 950 GPU 小時（≈40 天）；
外部 API 轉文字全年約 NT$21,000–50,000。地端符合「全程地端」的硬規則，
外部 API 需跨境傳輸評估。**尚未決定**。

### P-003　術語機械替換
`confirmed` 等級對映中，約 40 個變體非合法中文詞可安全機械替換，
5 個是合法中文詞不可盲替（例如某個詞在 4 次出現中有 3 次是它的字面意思）。
需人工分類 50 個變體的安全性後才能實作。**等全量跑完、
`unresolved_terms` 累積夠多時一次做**。

### D-005　移除 `intents` 欄位（已結案）
**日期**：2026-09-01
**決定**：`intents` 從 schema 移除，主類別改由 `call_category` 單獨承擔。
**過程**：同一批 42 通有效通話，三個版本實測 ——

| | v1.0 純 enum | v1.1 改名 | v1.2 附證據 |
|---|---|---|---|
| 空陣列比例 | 0% | 0% | 2% |
| 最高頻值佔比 | 67% | 76% | 74% |

**根因**：模型對詮釋型欄位從不留空（抽取型的 `mentioned_doctors` 為 85% 空）。
改 enum 名稱只是換一個承接「被迫填答」的垃圾桶值；要求附逐字稿原句後，
模型改為引用真實句子但掛到錯誤標籤，錯誤率不變。
**結論**：證據要求能擋「捏造文字」，擋不住「錯誤歸類」——
兩者不是同一個病，不能用同一招治。
**保留**：定義與完整理由存於 `labels.json` 的 `deprecated.intents`（未隨 repo 發布）。
