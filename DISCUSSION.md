# 第一份回應：對本專案四個提問的檢視

| | |
|---|---|
| **日期** | 2026-09-17 |
| **產出者** | Claude Code，Claude Opus 5 模型，reasoning effort: high |
| **對象** | [README 的〈想討論的問題〉](README.md#想討論的問題)四題 |
| **性質** | **AI 產出的批評，不是人類專家審閱** |

> ## 讀之前請注意
>
> 這份文件由 AI 產出。第 2 題問的正是「只跟 AI 討論會有什麼盲點」，
> 而回答者本身就是 AI —— **它有同一批偏誤，讀的時候要打折。**
> 它能指出這類系統常犯的錯，但看不見它自己看不見的東西。
>
> 放這份文件的用意不是證明專案被審查過，而是把「目前為止收到的批評」
> 攤開來。**真正的人類意見還沒有進來。** 如果你正在讀這份 repo 而且有經驗，
> 你的意見比底下整份文件都有價值。

---

## 1. 這條流程跟業界主流差多遠

**骨架是對的。** 音訊 → ASR → LLM 結構化抽取 → 聚合，這就是 conversation
intelligence 的標準形狀（Gong、CallMiner、Observe.AI 都是這個切法）。
沒有發明奇怪的東西。

**「每通一個 JSON」不是反模式，但用過頭了。** 當斷點續跑的檢查點它很好 ——
原子性、可讀、壞一個不影響其他。問題是它**同時被當成查詢層**，於是任何聚合
都要掃幾萬個檔。業界作法是分開：JSON 當不可變的落地層，再載進可查詢的東西。
這一步比想像中便宜，見第 4 題。

### 真正缺的一塊，不在軟體裡：單軌錄音

`cti_whisper_batch.py` 的 `SPEAKER_MODE` 註解寫得很清楚 —— 5 萬檔全是
gsm_ms 單軌，所以只能用 `none`，語者靠 LLM 從語境猜。

業界客服錄音的標準是**雙軌**（客服一軌、客戶一軌），語者分離變成零成本
且 100% 準確。

**這是整條管線品質的最大單一槓桿，而且不可能在下游補救。** 語者判斷錯，
`caller_sentiment`、`promised_callback`、`followups.owner` 全部跟著錯。
目前 prompt 裡整整一節「語者判斷線索」，在雙軌下可以直接刪掉。

CTI 系統通常設定得出雙軌錄音。**如果只能推動一件事，推這件。**

### 漏掉的階段：兩階段之間沒有品質閘門

有 `wav_integrity_scan`（很好，而且是被零填充壞檔咬過才有的），
但逐字稿 → LLM 之間沒有。一份全是 whisper 重複幻覺的逐字稿，會安靜地
變成一組看起來正常的標籤。**schema 保證 enum 合法，不保證語意合法。**

---

## 2. 只跟 AI 討論的盲點

### (a) AI 會幫你把事情做好，不會告訴你這件事不該做

AI 優化的是「下一步合理」，不是「整個方向對不對」。問怎麼改 prompt，
就會得到更好的 prompt；不會得到「這個欄位根本不該存在」。

### (b) 系統性偏向 prompt 層的解法

出問題時 LLM 的反射是調 prompt。D-005 正好是這個反射失效的案例 ——
加證據要求治不了錯誤歸類。**那是專案自己量出來的，不是 AI 提醒的。**

同一個偏誤的另一面：**AI 會少建議「別用 LLM」**。
`invalid_reason: 未接通` 不需要語言模型 —— 13~38 秒、內容 100% 是同一句
系統語音，用時長 + 音訊指紋就能濾掉，成本差三個數量級。

### (c) 沒有人扮演對手

有些欄位是**不可證偽**的：`caller_sentiment`、`service_quality_note`。
8 kHz 單軌逐字稿上的「情緒」，拿什麼當標準答案？人工標也標不一致。
**不可證偽的欄位會一直存在、一直被畫進報表，而且永遠不會被抓到錯。**

### (d) 「看起來合理」陷阱 —— 一個具體可查的問題

D-006 抽 20 筆、19 筆正確，記錄寫「95%，高於 7 成門檻」。
但 **n=20 的 95% 信賴區間下界大約是 76%**。資料只能支持
「真實準確率大於 76%」，不是「95%」。結論（高於 7 成）仍然成立，
但**理由比寫的弱很多**。要講 95%，樣本要到三位數。

這正是 AI 協作最典型的痕跡：**方法對、執行對、對不確定性的描述過度自信。**

### (e) 三個月後才爆炸的那種

| 失敗模式 | 為什麼現在看不出來 |
|---|---|
| **標籤變成事實** | D-001 擔心「半成品逐字稿被當成事實引用」所以不開放查詢。**但標籤是從同一份半成品推出來的，卻直接畫成報表。**同一個顧慮沒有套用到下游 |
| **術語表的分布偏移** | `glossary.json` 建立在單一院區單月的資料上。其他院區口音、慣用語不同，`confirmed` 等級會**自動套用**，可能主動改錯 |
| **無聲劣化** | 沒有監控標籤分布隨時間的變化。某個月「客訴風險」翻倍，是真的變多還是模型漂了？現在答不出來 |
| **未解釋的非確定性** | 3 通（0.08%）暴走生成被接受了，合理。但**機制沒查清楚**。沒解釋的隨機失敗通常會長大，而且是在換模型的那天 |

D-008 那個三方對照（同版雜訊 vs 跨版差異）是決策記錄裡方法論最漂亮的一段 ——
**它應該變成每次換模型都跑的常規檢查，而不是一次性的實驗。** 見第 4 題。

---

## 3. 個資與倫理

**先說做得好的：全程地端這件事，比同類專案的多數作法嚴格。**
不是客套 —— 大部分組織會直接把錄音丟給雲端 ASR。這裡守住了，
而且守在 P-002 那種「外部 API 全年只要兩三萬」的誘惑前面。

### 但重點的位置抓錯了

防護幾乎全部集中在「資料會不會外流」。而實際上最可能出事的地方是
**存取控制**，這一項等於沒有：所有逐字稿與分析結果，現在是一台
Windows 桌機使用者資料夾底下的普通檔案。誰能登入那台機器，
誰就有全部病患對話的全文。沒有帳號分權、沒有存取稽核、沒有加密。

**這比「會不會上雲」現實得多。**

### (1) 目的外利用 —— 最硬的一個

台灣個資法把病歷、醫療資料列為**特種個資**（§6）。原始錄音大概有告知
（「本通話將錄音」），而**服務品質確認**通常在原始蒐集目的內。

問題出在 `symptoms_mentioned`。prompt 裡寫明它是「**醫材上市後監督的訊號**」。
那不是服務品質，那是**醫療器材監督**，是一個新的特定目的。
個資法 §16 對特定目的外利用有嚴格要件。

**這一個欄位，比「病患不知道被 AI 分析」這件事本身風險高得多。**
「用 AI 而不是用人聽」通常不需要另外告知 —— 處理方法變了，目的沒變。
但**目的變了就要另外處理**。

具體建議：去問法遵「原始錄音告知書上寫的蒐集目的是什麼」。
**那張紙決定了能做什麼，不是技術決定的。**

### (2) 員工監控

`service_quality_note` 在 prompt 裡寫「只記錄客觀可改善之處，不要評價人員」——
那是**政策控制，不是技術控制**。資料本身仍然是逐通、可歸屬到分機、
分機可歸屬到人。D-002 不指派負責人是好的，但它擋的是報表，不是資料。

台灣實務上，員工監控要經**勞資會議或工會告知**。技術上可以做的：
在寫入前就聚合掉（只存院區層級的彙總），或在有政策之前先關掉這個欄位。

### (3) 沒想到的：分析結果比錄音更危險，而且會變成證據

- **再識別**：報表的電話遮蔽了，但 `mentioned_doctors` + 日期 + 院區的
  組合再識別性極高。**遮電話不等於去識別化。**
- **可搜尋的文字比音檔危險得多。** D-001 決定不建全文索引，直覺是對的。
  但檔案就在那裡，任何人 `grep` 一下就有了全文檢索。**不建索引不是控制措施。**
- **`has_complaint_risk=true` 是一筆「本院已知悉」的書面紀錄。**
  如果某通後來變成醫療爭議或訴訟，對方在證據開示時拿到這筆標籤，
  它證明的是：系統當時就標記了這通有客訴風險，而且沒有人處理
  （因為 D-002 明確決定不指派負責人、不做追蹤）。

  **D-002 在倫理上是對的決定（不製造假的追蹤感），在法務上可能是最糟的組合：
  留下知悉紀錄，同時明確決定不行動。** 這兩件事是分開想的，合起來看才看得到。
  這不是說 D-002 錯，是說這個交互作用需要法務看一眼。

### (4) 還缺的

保留與銷毀排程（P-001 已記著）、存取稽核紀錄、個資影響評估（DPIA）。

---

## 4. PoC → 上線，先補哪三件

先講一句反方向的：**現階段不需要資料庫，也不需要 Airflow。**
PoC 的範圍收斂得很好，不要在這個階段把它工程化。
以下三件按「不做會讓後面全部白做」排序。

### 第一件：執行清單（run manifest）

**沒有這個，量到的每一個數字都不可重現。** D-008 那個三方對照花了整天，
而它回答的問題是「這兩批資料可比嗎」—— 如果每批輸出都帶著自己的身分證，
那個問題是查表，不是做實驗。

已經做了一半（每次執行存 prompt 快照）。補完：

```json
{
  "run_id": "202509_A_20260917_1432",
  "code_git_sha": "4be57bf",
  "prompt_sha256": "...",
  "labels_version": "1.2",
  "glossary_version": "0.3",
  "model": "qwen3.8",
  "model_digest": "sha256:...",
  "num_ctx": 32768,
  "input_count": 3934,
  "started_at": "...", "finished_at": "..."
}
```

每筆輸出帶 `run_id`。**成本大概兩小時，回報是永久的。**
這件事要最先做，因為之後產生的每一批資料都需要它，**而已經跑完的那些補不回來。**

### 第二件：雙軌錄音（往上游推）

理由見第 1 題。放在第二件不是因為不重要，是因為它**不在開發者手上** ——
要跨部門、要等新資料累積。**所以要現在就開始問，不是等 PoC 結束才問。**

### 第三件：把 D-008 變成常規檢查

正確的方法已經發明出來了，只是被當成一次性實驗。把它固定下來：

- 選 **50 通**固定的 canary（涵蓋長短、各類別、含已知會失敗的那 3 通）
- 換模型／改 prompt／改 `labels.json` **就跑一次**
- 斷言：類別分布在容忍區間內、enum 一致率不低於同版雜訊基線
- **永遠不要斷言 LLM 的確切輸出**

這回答了「輸出不確定的管線要怎麼測」。答案是：**測統計性質，不測個案。**

另外，確定性的部分要用一般單元測試蓋掉，而且很便宜：
`parse_meta()` 的檔名解析、D-004 的號碼正規化規則、WAV 檔頭讀取。
**這些是純函數，壞了會安靜地毀掉所有下游 —— D-004 實測比對失敗率 100% 就是例子。**

### 其他幾件（順序靠後）

| 項目 | 建議 |
|---|---|
| 查詢層 | 別上 Postgres，先用 **DuckDB**。單檔、免伺服器、可直接查現有 JSON，一個下午能接上。等真的需要並行寫入再談資料庫 |
| 排程告警 | Windows 工作排程器 + 心跳檔就夠。告警條件：批次沒在預期時間結束、失敗率超標、GPU 不見了、輸出筆數對不上輸入。**不要建 Airflow** |
| 擴展 | 瓶頸是 GPU 小時（全年約 950）。省的方法不是換更快的卡，是**別把貴模型花在便宜的通話上**。把「只深入分析高單價對話」做成正式的分級管線：規則先濾掉未接通與無效，中等模型做分類，只有需要細節的才上大模型 |

---

## 如果只能記一句

**這個專案的工程紀律比統計紀律好很多。**

斷點續跑、帳本、prompt 快照、版本比對 —— 這些做得比多數 PoC 紮實。
但 n=20 講 95%、不可證偽的欄位、沒有分布監控，這些是同一類問題：
**對「我怎麼知道這是對的」這個問題，投入得比對「怎麼把它跑完」少。**

---

## 還沒有人回答的

這份文件只是 AI 的一份意見。以下仍然開放，歡迎開 issue：

- 上面第 3 題的法遵判斷**沒有經過任何法律專業人士確認**，只是根據公開條文的推論
- 沒有實際在醫療場域做過語音分析的人看過這條流程
- 沒有人檢查過程式碼本身的正確性 —— 上面全部是架構層級的意見
